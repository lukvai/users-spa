# To run application in production mode

- Run `npm install`
- Run `npm run build`
- Run `npm run preview`

# To run application in dev mode

- Run `npm install`
- Run `npm run dev`

# Note
This application is WIP
